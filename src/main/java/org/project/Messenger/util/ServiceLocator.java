package org.project.Messenger.util;

import org.project.Messenger.service.MessageService;
import org.springframework.context.ApplicationContext;
import org.springframework.context.support.ClassPathXmlApplicationContext;

public class ServiceLocator {

	/*
	 * ApplicationContext context = new
	 * ClassPathXmlApplicationContext("applicationContext.xml"); public
	 * MessageService messageService =
	 * context.getBean("messageService",MessageService.class);
	 * 
	 * public static MessageService getBean() { return messageService; }
	 */
}
